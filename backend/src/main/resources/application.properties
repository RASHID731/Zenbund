spring.application.name=backend

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/zenbund
spring.datasource.username=vanilla
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# Cloudinary Configuration
# Get these values from: https://cloudinary.com/console
cloudinary.cloud-name=diealq2zf
cloudinary.api-key=311126638957975
cloudinary.api-secret=sSqKKqIvX_r_tks9NJq7bIMfTak

# File Upload Configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=50MB

# JWT Configuration
# Secret key for signing JWT tokens (must be at least 256 bits / 32 characters)
# In production, use a strong randomly generated secret and store securely (e.g., environment variable)
jwt.secret=zenbund-super-secret-jwt-key-for-authentication-must-be-at-least-256-bits-long
# Token expiration: 24 hours in milliseconds (1000ms * 60s * 60m * 24h)
jwt.expiration=86400000
